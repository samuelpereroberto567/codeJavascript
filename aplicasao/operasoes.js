const numero = 5;
const texto = "5";
console.log(numero === texto)

//typeof
console.log(typeof numero)
console.log(typeof texto)
/*
O operador de dois iguais (==) realiza a comparação 
e também faz uma conversão implícita antes de executar essa comparação.
o operador de três iguais (===) é mais rigoroso e sempre compara os valores diretamente, 
sem realizar qualquer conversão. Portanto, um número e uma 
string não são considerados iguais quando usamos o operador de três iguais.

!= e !==: Operadores “não igual” 
utilizados para comparação, da mesma forma que == e === retornam true ou false.
*/
// boas praticas sao ===
